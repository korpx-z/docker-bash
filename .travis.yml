arch: s390x
services:
  - docker
env:
  global:
    - TEST_DIR="$PWD/test/devel/"
    - TEST_SCRIPT="test.sh"
    - BUILD_DIR="$PWD/devel/"
    - REPO="quay.io/ibmz/bash"
    - TAG="$REPO:devel"
before_script:
  - docker login quay.io -u $ROBOT -p $ROBOT_TOKEN
  - cd $TEST_DIR && chmod +x $TEST_SCRIPT
script:
  - cd $BUILD_DIR && docker build . --tag $TAG
  - cd $TEST_DIR && bash $TEST_SCRIPT
after_script:
  - docker push $TAG

